#ifndef STATES_H
#define STATES_H
/**
 * @brief The States enum keeps the state of the project running
 */
enum class States {

    Idle,
    Imported,
    Opened,
    Saved,
    Exported,
    Closed
};

#endif // STATES_H
